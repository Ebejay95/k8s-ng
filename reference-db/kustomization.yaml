apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Zentrale Stammdaten-/Referenz-DB als In-Cluster-StatefulSet.
# Admin-App = Owner (RW), Tenant-Apps = read-only (reference_reader).
resources:
  - 00-namespace.yaml
  - 10-configmap.yaml
  - 15-init-configmap.yaml
  - 20-externalsecret.yaml
  - 30-statefulset.yaml
  - 40-service.yaml
  - 50-networkpolicies.yaml
