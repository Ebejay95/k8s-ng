apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Lokales In-Cluster-MinIO als S3-Backup-Ziel fuer Velero (Docker Desktop).
# In prod/staging laeuft MinIO/S3 SEPARAT vom Cluster (Terraform) -> dieses
# Modul wird NUR im local-Overlay eingebunden.
resources:
  - 00-namespace.yaml
  - 10-secret.yaml
  - 20-statefulset.yaml
  - 30-service.yaml
  - 40-create-buckets-job.yaml
  - 50-networkpolicies.yaml
