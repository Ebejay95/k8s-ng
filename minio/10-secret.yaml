apiVersion: v1
kind: Secret
metadata:
  name: minio-credentials
  namespace: minio
  labels:
    app: minio
type: Opaque
# NUR fuer den lokalen Docker-Desktop-Betrieb (MinIO als Velero-Backup-Ziel).
# In prod laeuft MinIO/S3 SEPARAT vom Cluster (per Terraform) und die
# Zugangsdaten kommen aus Vaultwarden via ExternalSecret.
stringData:
  root-user: minioadmin
  root-password: minioadmin123
