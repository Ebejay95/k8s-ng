apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Admin-Base (Control-Plane). Eigener Namespace, eigene DB, eigener Host.
# Serviciert KEINE Kundendaten – verwaltet nur die zentrale Tenant-Registry.
resources:
  - 00-namespace.yaml
  - 10-configmap.yaml
  - 21-externalsecret.yaml
  - 30-deployment.yaml
  - 40-service.yaml
  - 50-ingress.yaml
  - 60-hpa.yaml
  - 65-pdb.yaml
  - 70-networkpolicies.yaml
  - 80-limits-and-quotas.yaml
