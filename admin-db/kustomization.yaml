apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Zentrale Admin-/Registry-DB als In-Cluster-StatefulSet im navosec-admin
# Namespace. Owner = navosec_admin (RW, nur die Admin-App).
resources:
  - 10-configmap.yaml
  - 20-externalsecret.yaml
  - 30-statefulset.yaml
  - 40-service.yaml
  - 50-networkpolicies.yaml
